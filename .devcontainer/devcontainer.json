{
  "name": "bin-collection-website",
  "dockerComposeFile": ["../docker-compose.yml"],
  "service": "app",
  "workspaceFolder": "/workspace",
  "forwardPorts": [3000],
  "postCreateCommand": "npm ci || npm install",
  "remoteUser": "node"
}
